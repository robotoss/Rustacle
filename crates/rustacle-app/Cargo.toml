[package]
name = "rustacle-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
rustacle-kernel = { path = "../rustacle-kernel" }
rustacle-ipc = { path = "../rustacle-ipc" }
